apiVersion: batch/v1
kind: CronJob
metadata:
  name: milwaukee-cronjob
  labels:
    {{- include "trading.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.milwaukee.schedule | quote }}
  timeZone: {{ .Values.milwaukee.timeZone }}
  jobTemplate:
    spec:
      template:
        metadata:
          {{- with .Values.milwaukee.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          labels:
            {{- include "trading.labels" . | nindent 12 }}
            {{- with .Values.milwaukee.podLabels }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
        spec:
          {{- with .Values.milwaukee.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: {{ .Chart.Name }}
              image: "{{ .Values.milwaukee.image.repository }}:{{ .Values.milwaukee.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.milwaukee.image.pullPolicy }}
              resources:
                {{- toYaml .Values.milwaukee.resources | nindent 16 }}
              {{- with .Values.milwaukee.volumeMounts }}
              volumeMounts:
                {{- toYaml . | nindent 16 }}
              {{- end }}
          {{- with .Values.milwaukee.volumes }}
          volumes:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: {{ .Values.milwaukee.restartPolicy }}
